# Railway build + start config.
# Serves the Next.js admin dashboard AND the combined worker host
# in a single container (optimized for the $5 plan).

[phases.build]
cmds = ["npx prisma generate", "npx prisma migrate deploy", "npx next build"]

[start]
cmd = "npm run prod:start"
